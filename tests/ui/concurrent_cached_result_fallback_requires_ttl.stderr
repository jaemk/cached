error: `result_fallback` requires entries that expire: set a TTL (`ttl`/`ttl_secs`/`ttl_millis`, e.g. `ttl_secs = 60`) or `expires = true` for per-value expiry. It serves the last cached `Ok` value when a refresh returns `Err`, but a refresh only happens after an entry expires. Without expiry the function body is never re-run for a cached key and the fallback can never fire - making the option a no-op.
 --> tests/ui/concurrent_cached_result_fallback_requires_ttl.rs:7:4
  |
7 | fn my_fn(x: u32) -> Result<u32, String> {
  |    ^^^^^

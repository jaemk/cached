error: `expires` and `ttl` are mutually exclusive - `expires` delegates expiry to the value via the `Expires` trait; `ttl` applies a uniform time-based TTL to all entries
 --> tests/ui/cached_expires_malformed_ttl.rs:8:4
  |
8 | fn my_fn(x: u32) -> u32 {
  |    ^^^^^

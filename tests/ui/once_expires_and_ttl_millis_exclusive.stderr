error: `expires` and `ttl_millis` are mutually exclusive - `expires` delegates expiry to the value via the `Expires` trait; `ttl_millis` applies a uniform millisecond TTL to all entries
 --> tests/ui/once_expires_and_ttl_millis_exclusive.rs:3:24
  |
3 | #[once(expires = true, ttl_millis = 500)]
  |                        ^^^^^^^^^^
